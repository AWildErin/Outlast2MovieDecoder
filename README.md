# Outlast 2 Movie Decoder
Converts .ol2 movies to webm (Currently does not add audio to the movie)

## Requirements
ffmpeg dlls must be installed into `(ExeDir)/ffmpeg/*.dll`

## Usage
`outlast2moviedecoder.exe (file)`
